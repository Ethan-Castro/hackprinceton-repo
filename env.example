# Cerebras API configuration (required for Cerebras models & carbon comparisons)
CEREBRAS_API_KEY=your_cerebras_api_key_here
# Optional: override the model used for token estimation (defaults to llama-3.3-70b)
# CEREBRAS_CARBON_MODEL=llama-3.3-70b

# Vercel AI Gateway Configuration (required)
# Get your API key from https://vercel.com/docs/ai-gateway
# If not set, base URL defaults to https://ai-gateway.vercel.sh/v1/ai
AI_GATEWAY_API_KEY=your_gateway_api_key_here
# AI_GATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1/ai

# v0 API Key (REQUIRED for v0 Studio - Education, Health, Business, Sustainability)
# Get your API key from https://v0.dev/chat/settings/keys
# Without this, the v0 Studio features will not work
V0_API_KEY=your_v0_api_key_here

# NextAuth Configuration (required for Textbook Studio authentication)
# Generate a random secret: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your_nextauth_secret_here
# Alternatively, you can use AUTH_SECRET instead of NEXTAUTH_SECRET
# AUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Vercel OIDC Token (auto-managed by vc dev)
# VERCEL_OIDC_TOKEN=auto_generated_when_using_vc_dev

# Neo4j (required for trackers and graph storage)
# Example: bolt+s://<your-instance>.databases.neo4j.io
NEO4J_URI=
NEO4J_USERNAME=
NEO4J_PASSWORD=


# Model Context Protocol (MCP) Configuration for Direct MCP Integration
# Transport type: http, sse, or stdio
MCP_TRANSPORT=http

# For HTTP/SSE transport:
MCP_SERVER_URL=
MCP_API_KEY=

# For stdio transport (local development):
# MCP_COMMAND=node
# MCP_ARGS=path/to/your/mcp/server.js

# Google Docs Integration (optional - for readGoogleDoc and getGoogleDocMetadata tools)
# Path to Google service account credentials JSON file
# Required for using Google Docs tools
# Generate credentials from: https://console.cloud.google.com/apis/credentials
# GOOGLE_CREDENTIALS_PATH=/path/to/service-account-key.json

# Gamma API Integration (optional - for presentation and document generation)
# Get your API key from: https://gamma.app
# Used for generating presentations, documents, and webpages
GAMMA_API_KEY=sk-gamma-your-api-key-here

# Neon Database (PostgreSQL) Configuration
# ⚠️  IMPORTANT: Copy one of these connection strings to your .env.local file
# Get your connection strings from your Neon dashboard: https://console.neon.tech

# Recommended for most uses (with connection pooling via pgbouncer)
# Use this for server-side operations, API routes, and server components
# DATABASE_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/database?sslmode=require

# For uses requiring a connection without pgbouncer (transactions, prepared statements)
# Use this when you need unpooled connections for better transaction isolation
# DATABASE_URL_UNPOOLED=postgresql://user:password@host.region.aws.neon.tech/database?sslmode=require

# Alternative: Vercel Postgres format (also supported)
# If you're using Vercel Postgres, use these instead:
# POSTGRES_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/database?sslmode=require
# POSTGRES_URL_NON_POOLING=postgresql://user:password@host.region.aws.neon.tech/database?sslmode=require
# POSTGRES_USER=your_username
# POSTGRES_HOST=your_host
# POSTGRES_PASSWORD=your_password
# POSTGRES_DATABASE=your_database
# POSTGRES_URL_NO_SSL=postgresql://user:password@host-pooler.region.aws.neon.tech/database
# POSTGRES_PRISMA_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/database?connect_timeout=15&sslmode=require

# Parameters for constructing your own connection string (optional)
# PGHOST=your_host-pooler.region.aws.neon.tech
# PGHOST_UNPOOLED=your_host.region.aws.neon.tech
# PGUSER=your_username
# PGDATABASE=your_database
# PGPASSWORD=your_password

# Neon Auth environment variables for Next.js (optional)
# Get these from your Neon dashboard if using Neon Auth
# NEXT_PUBLIC_STACK_PROJECT_ID=your_project_id
# NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=your_publishable_key
# STACK_SECRET_SERVER_KEY=your_secret_server_key

# E2B API Key (required for Python code execution in Business Analyst)
# Get your API key from: https://e2b.dev
# Used for executing Python code with data science libraries (NumPy, Pandas, Matplotlib, etc.)
E2B_API_KEY=your_e2b_api_key_here

# Exa Search API (optional - for web search in Business Analyst)
# Get your API key from: https://www.exa.ai
EXA_API_KEY=your_exa_api_key_here

# Firecrawl API (optional - for website scraping in Business Analyst)
# Get your API key from: https://firecrawl.dev
FIRECRAWL_API_KEY=your_firecrawl_api_key_here
# Also used for Open Lovable search/scrape experiences under /open-lovable

# Parallel AI API (optional - for parallel processing in Business Analyst)
# Get your API key from: https://parallel.com
PARALLEL_API_KEY=your_parallel_api_key_here

# SendGrid Email Configuration (optional - for health reminders via email)
# Get your API key from: https://sendgrid.com
# Used for sending appointment reminders, medication reminders, lab notifications, and health summaries
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=noreply@healthgateway.ai

# ElevenLabs Text-to-Speech (optional - for health reminders via voice calls)
# Get your API key from: https://elevenlabs.io
# Used for generating voice reminders for appointments and medications
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Cloudinary (for image upload and delivery)
# Sign up at https://cloudinary.com and create API keys in your dashboard
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
# For client-side components (e.g. next-cloudinary)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
